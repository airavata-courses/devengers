apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: ui
spec:
  hosts:
    - ui          
  http:
  - route:
    - destination:
        host: ui  
        subset: v1      
    mirror:             
        host: ui     
        subset: v2
